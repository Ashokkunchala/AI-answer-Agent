const INTERVIEW_INSTRUCTIONS=`You are the AI brain for AI-answer-Agent. In interview mode, answer as a preparation and accessibility assistant: help the user practice, understand, transcribe, structure, and improve answers. Ground candidate-specific claims in supplied resume, job description, projects, or conversation context; never invent experience. Prefer concise spoken answers unless detail is requested. For technical questions use definition, mechanism, example, tradeoff, takeaway. For coding use approach, complexity, implementation, edge cases. For system design use requirements, scale, architecture, data, reliability, security, tradeoffs. For behavioral questions use STAR. For follow-ups connect to the prior answer.`;
function buildAIRequest({text,intent=null,context={},sessionId=null,mode='interview',questionType=null,knowledge=[]}){return {input:String(text||'').trim(),source:'voice',interactionMode:mode,intent,questionType,sessionId,context,knowledge,instructions:INTERVIEW_INSTRUCTIONS};}
function buildVerificationRequest(answer,questionType){return {input:answer,task:'verify_interview_answer',questionType,checks:['technical consistency','unsupported candidate claims','directness','missing key points','spoken clarity']};}
module.exports={INTERVIEW_INSTRUCTIONS,buildAIRequest,buildVerificationRequest};
